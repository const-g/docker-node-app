{
  "name": "docker-node-app",
  "version": "1.0.0",
  "description": "a first app using docker",
  "scripts": {
    "dev": "nodemon --watch server server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.15.2"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
